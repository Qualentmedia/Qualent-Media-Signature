{
  "name": "gsc-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Google Search Console analytics dashboard with US-default keyword/query reporting and 7-day impression-drop alerts.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
